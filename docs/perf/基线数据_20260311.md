# 性能基线（2026-03-11）

本基线用于后续每个优化点的对比验证，口径固定为：CPU、内存、RT、QPS、错误率。

## 0. 环境
- Python：`C:/anaconda3/envs/Gamma/python.exe`（3.12）  
- 服务：`apps/crypto_screener/app/web_server.py`（本地启动，端口 8001）
- 压测脚本：`tools/perf/bench_http.py`
- 备注：本次为本地复现实验，启用 `QC_DISABLE_AUTH=1` 仅用于压测免登录（默认关闭，不影响线上）。

## 1. latest_enriched 基线
- 原始结果文件：[baseline_latest_enriched.json](file:///d:/量化交易/中性1.8/tools/perf/baseline_latest_enriched.json)
- 结论摘要：
  - VU=50、每用户3请求：P95 ≈ 99.816ms，QPS ≈ 451.907，错误率 0。

## 2. kline 基线
- 原始结果文件：[baseline_kline.json](file:///d:/量化交易/中性1.8/tools/perf/baseline_kline.json)
- 结论摘要：
  - VU=50、每用户5请求：P95 ≈ 1055.757ms，QPS ≈ 91.876，错误率 ≈ 0.4%（存在偶发超时/异常）。

